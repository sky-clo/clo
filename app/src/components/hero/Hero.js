import React from "react";

import styles from "./hero.module.scss";

export default function Hero() {
  return (
    <section className={styles.section}>
      <h1 className="c-heading-alpha">Where would you like to go?</h1>
    </section>
  );
}
